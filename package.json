{
  "name": "tech-news-site",
  "version": "1.0.0",
  "description": "Minimal news site for AI, developer tools, and tech industry news",
  "main": "fetch-news.js",
  "scripts": {
    "start": "npx serve public",
    "fetch": "node fetch-news.js && cp news-data.json public/news-data.json",
    "build": "node fetch-news.js && cp news-data.json public/news-data.json",
    "dev": "npx serve public"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  }
}
